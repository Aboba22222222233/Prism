# 🚀 Prism: Developer Onboarding Guide

Добро пожаловать в проект **Prism**! Этот документ предназначен для быстрого ввода новых разработчиков в курс дела. Здесь описана архитектура, стек технологий и структура всего 
репозитория (Web, Mobile, Backend).

---

## 🏗 Архитектура проекта (Monorepo-style)

Проект разделен на три логические части, которые хранятся в одном репозитории:

1. **`src /` (Web-приложение)**: React + Vite + TailwindCSS. Предназначено в первую очередь для **Учителей** (Teacher Dashboard) и администраторов.
2. **`prism-mobile /` (Mobile-приложение)**: React Native + Expo Router. Предназначено для **Учеников** (дневники, задания, аналитика) и частично для учителей (менторинг).
3. **`supabase /` (Backend)**: База данных PostgreSQL, Edge Functions (для работы с ИИ) и хранилище файлов (Storage).

---

## 🛠 Технологический стек

*   **Frontend (Web)**: React, Vite, Tailwind CSS, Lucide React (иконки), Recharts (графики), xlsx (парсинг отчетов).
*   **Mobile (iOS/Android)**: React Native, Expo, Expo Router (навигация на базе файлов), NativeWind/StyleSheet.
*   **Backend & BaaS**: Supabase (Auth, Database, Storage, Edge Functions).
*   **AI Integration**: Провайдер Groq Cloud (через официальный SDK или напрямую). Модели: Llama-3 или chatgpt 120b oss. Обрабатывает анализ эмоций и чат-бота.
*   **Язык**: TypeScript (strict mode) по всему стеку.

---

## 📂 Структура папок и ключевые файлы

### 🌐 1. Веб-приложение (`/src`)
Веб-версия имеет классическую структуру React-приложения.

*   `src/pages/`: Основные экраны.
    *   `TeacherDashboard.tsx`: Главная страница учителя. Самый большой и важный файл в вебе. Содержит логику создания классов, просмотра учеников, AI-аналитики и выгрузки полного отчета в `.xlsx`.
    *   `Login.tsx` / `Register.tsx`: Экраны аутентификации.
    *   `Landing.tsx`: Главная рекламная страница проекта.
*   `src/components/`: Переиспользуемые UI элементы (Navbar, Layout).
*   `src/lib/`:
    *   `supabase.ts`: Инициализация клиента Supabase для веба.
    *   `gemini.ts`: Файл для прямого общения с AI (если не используется Edge Function).

### 📱 2. Мобильное приложение (`/prism-mobile`)
Мобильное приложение построено на Expo Router (как Next.js, только для мобилок).

*   `app/`: Папка с роутами (экранами).
    *   `_layout.tsx`: Корневой файл приложения. Здесь инициализируются Push-уведомления (`useNotifications.ts`) и проверяется сессия.
    *   `login.tsx`: Логика входа и распределения ролей (Учитель или Ученик). Вызывает базу данных (RPC `verify_teacher_code`) для проверки прав учителя.
    *   `(student)/`: Вкладки и экраны для ученика.
        *   `dashboard.tsx`: Главный экран (Дневник, Статистика, Задания).
        *   `checkin.tsx`: Форма создания новой записи (Выбор настроения, эмоций, слайдеры сна).
    *   `(teacher)/`: Вкладки и экраны для учителя в телефоне. Облегченная версия веб-дашборда.
        *   `classes.tsx`: Список классов.
        *   `class/[id].tsx`: Карточка конкретного класса, чат с классом и ИИ-сводка.
*   `src/`:
    *   `components/ui/`: `Button.tsx`, `Card.tsx`, `Input.tsx` — кастомные UI-компоненты с учетом системной светлой/темной темы (`ThemeContext.tsx`).
    *   `lib/`:
        *   `supabase.ts`: Настройка Supabase с использованием `expo-secure-store` для безопасного хранения токенов.
        *   `useNotifications.ts`: Хук для реализации локальных "Wow" Push-уведомлений (напоминания).

### 🗄 3. База Данных и Бэкенд (`/supabase` & DB Schema)
Вся логика защиты опирается на PostgreSQL Row Level Security (RLS) в Supabase.

*   **Таблицы**:
    *   `profiles`: Хранит роль (`teacher` / `student`) и ФИО.
    *   `checkins`: Эмоциональные дневники.
    *   `classes` / `class_enrollments`: Связи учителей и учеников.
*   **Ключевые политики безопасности (RLS)**:
    *   Студенты могут делать `SELECT` только своих записей дневников.
    *   Учителя могут делать `SELECT` только тех дневников учеников, которые состоят в их классе (проверка через джоины в RLS).
    *   `admin_config`: Хранит секретный код учителя. Доступ закрыт для прямых чтений. Доступ идет через безопасную функцию `verify_teacher_code(input_code)`.
*   **Edge Functions (`supabase/functions/`)**:
    *   `chat-ai`: Выполняет обращения к Groq Cloud API. **Ключи хранятся здесь (в секретах Supabase)**, а не в клиентском коде, чтобы предотвратить их кражу энтузиастами.

---

## 🏃‍♂️ Как запустить локально

### Общие требования
- Установлен `Node.js` (рекомендуется v20+) и `npm`.
- Выбран инструмент: Web (браузер) или Mobile (Приложение Expo Go на телефон / Симулятор iOS/Android).
- Наличие файлов `.env` (Брать у тимлида, в них урлы к базе данных). Они находятся в корне и в папке `prism-mobile` и игнорируются git (`.gitignore`).

### Запуск Веб-приложения
```bash
# В корне проекта
npm install
npm run dev
# Откроется на http://localhost:5173
```

### Запуск Мобильного приложения
```bash
cd prism-mobile
npm install
npm start # или npx expo start
# Отсканируйте QR-код в приложении Expo Go на своем смартфоне
```

---

## 🔐 Важно: Security First
1. **Никогда не хардкодьте ключи API** в файлах `src/` или `prism-mobile/app/`. Взломают в первый же день релиза в сторах.
2. Прокачку прав (ролей) нужно проводить **строго через базу данных** (наши RPC функции), а не доверять тому, что присылает клиент.
3. Относитесь к базе `Supabase` с уважением: тщательно проверяйте правила RLS перед каждым деплоем в production.

Удачного кодинга! Если вы здесь, то мы скоро поменяем систему образования 🚀
